# Logica Context — VS Code Copilot Instructions

Use Logica Context MCP tools to protect the context window:
- `lctx_batch_execute` for research
- `lctx_search` for follow-up queries
- `lctx_execute` for sandboxed code execution
- `lctx_fetch_and_index` for fetching URLs
