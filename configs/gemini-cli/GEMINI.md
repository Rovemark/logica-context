# Logica Context — Gemini CLI Configuration

Use Logica Context MCP tools to protect context window:

1. `lctx_batch_execute(commands, queries)` — Primary research tool
2. `lctx_search(queries)` — Follow-up questions
3. `lctx_execute(language, code)` — Sandboxed execution
4. `lctx_fetch_and_index(url)` — Fetch and index URLs
